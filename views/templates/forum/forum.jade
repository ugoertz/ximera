div
  div.btn-toolbar(role="toolbar")
    div.btn-group
      button.btn(ng-click="replying = !replying", ng-class="{active:replying}", ng-show="!replying && (user.current)")  <i class="fa fa-reply"></i>&nbsp;Stelle eine Frage.  Gib einen Kommentar ab.
  div(reply="",ng-if="replying",parent="", forum-name="{{forum}}", reply-done="$parent.replying = false;")
  div.forum(thread="", posts="toplevel", parent="", forum-name="{{forum}}")
